# CCCaster_v10 開発ルール

## 最初に読む

- 現行仕様・確認状況: [docs/CURRENT_STATE.md](docs/CURRENT_STATE.md)
- 未解決事項: [docs/OPEN_ISSUES.md](docs/OPEN_ISSUES.md)
- 作業場所・スキル・復帰方法: [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)
- 現在のコードを最優先に照合する。日付付き設計書は当時の記録。`docs/design/master/`は2026-08-13の旧監査で、現行仕様ではない。

## ユーザーとの約束

- 人数の指定がない作業は単一エージェントで行う。ユーザーがサブエージェント人数を指定した場合はその人数で作業する。同時稼働上限がある場合は順番に実行し、作成自体が制限された場合は実人数を明示する。
- 応答、コミットメッセージ、changelogは日本語。
- テストは変更に必要な範囲に絞る。既存の実ゲーム／待受プロセスを無断で停止せず、検証で起動した対象のみ終了する。
- 未コミット変更、コントローラ設定、実ゲームデータを保全する。
- 実装済み／実機確認済み／未達を区別する。設定周期や平均FPSを瞬間的な実測値の代わりにしない。

## 壊してはいけない境界

- 対象はMBAACC Ver.1.07 Rev.1.4.0、32bit。別版へ固定アドレスを流用しない。
- Steam版は独立プロジェクト `I:/work_space/CCCaster_Steam` で管理する。ゲームの微差によりクロスプレイは対象外。このフォルダーへSteam移植コードを混在させない。
- 生入力は独立した入力時計で採取し、フレーム番号と中央バッファで管理。ゲーム状態の書込み・保存・復元はゲームスレッド上で行う。
- `InputInjector.hpp`の旧直接書込みは使用禁止。既存のGameMem入力経路を用い、ゲームが生成する派生入力状態へ横から書かない。
- 通常更新は処理・描画・待機を含め60Hzの絶対締切へ合わせる。処理後に16.667msを追加で待たせない。
- WASAPIへ無音を継続供給し、起動位置0を時計として採用しない。故障時のQPC切替を隠さない。全フレーム誤差ゼロは未達。
- D>=0、R>=0、D+R<=8。既定D2/R4。通信版10。既存の世代境界・RNG合流・予測上限・再送を保つ。
- 入力待ちの時間制限は通常3秒、起動・合流30秒。旧監査の「ゲームスレッド待機禁止」は現行方式には適用しない。
- ロールアップは内部を高速再計算し、中間画像のPresentを省略する。通常ゲームの前進と再計算を計測上も分ける。
- `CC_SKIP_FRAMES_ADDR`へは書き込まない。描画省略は既存APIフック側で行う。
- F4設定中のゲーム入力遮断、未選択時B抑止、双方ONCEでのみ再戦、片側キャラセレ優先、リプレイ保存無効を維持する。

## ビルドと記録

- Windowsは `C:/msys64/mingw32/bin` の32bitツールチェーンを使用。CMakeCacheを別Worktreeからコピーしない。
- 検証・配布は `cccaster-verify` スキルの手順を使用。書類だけの変更に実ゲームテストを課さない。
- Linuxのharnessは同期ロジック用。Windowsの音声時計や実機入力精度を検証した扱いにしない。
- 変更に対応する `changelogs/CHANGELOG_YYYY-MM.md` を更新。コミットする場合、先頭の変更見出しと日本語コミットメッセージを合わせる。
- 現行挙動が変わったらCURRENT_STATE、未解決事項が変わったらOPEN_ISSUESを更新し、根拠ログへの参照を残す。
