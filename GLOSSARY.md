# CCCaster_v10 ドメイン用語集 (Glossary)

本プロジェクト（CCCaster_v10）におけるシステム・通信・ゲーム固有の専門用語の定義です。
これらの用語は設計書やソースコードの変数名において、**定義通りの意味で統一**して使用しなければなりません。

## コア通信・同期関連

### Rollup (ロールアップ)
ゲームの最新状態に追いつくため、遅れて届いた入力パケットを適用しながら、バックグラウンドまたは高速描画スキップを利用して複数フレーム分の処理を一気に計算して進めること。一般には「ロールバック後のローカル再計算」を指します。

### Redundant Protocol (冗長化プロトコル / CCTRパケット)
UDPの特性であるパケットロスを自己復旧させるため、1つの通信パケット（最大51バイト）の中に**現在フレームを含む過去10フレーム分の入力履歴**を含めて送信するプロトコルの名称。パケットの先頭マジックナンバー `"CCTR"` に由来します。

### OWD (One-Way Delay / 片道遅延時間)
送信側の発信時刻から受信側の到達時刻までのネットワーク上の片道遅延時間。本システムではNTPの概念を応用して双方向の時刻オフセット（θ: クロックドリフト）を算出し、仮想時計 (VirtualClock) の誤差修正に用います。

### VirtualClock (仮想クロック)
Windows標準のミリ秒タイマーでは精度が足りないため、QueryPerformanceCounter (QPC) 等の高精度OSタイマーを利用し、両PC間で完全に同期された「今ゲーム内ではいつ（何μs経過したか）」を表現するための論理タイマー機構。

## ゲームインタフェース・アーキテクチャ関連

### FastBoot (高速ブート / The Launcher)
ランチャー層 (`GameLauncher`) がMBAA.exeをサスペンド状態で起動し、コアDLL (`cccaster_hook.dll`) をインジェクトする仕組みのこと。インジェクト完了後、プロセスをResumeすることでゲーム側の起動フローを制御下に置きます。

### SaveState / LoadState (ステート保存・復元)
ロールバックエンジンの要となる機能で、その瞬間のゲームエンジンのメモリヒープ・静的変数等の状態（StateBuffer）をスナップショットとしてメモリに丸ごとコピー保存し、後から完全に同じ状態へと巻き戻す（直接メモリを上書きする）仕組み。

### DummyPeer (ダミーピア)
実際の対戦相手を用意すること無く、1台のPCのみで通信プロトコル・ロールバックを検証するために作られた専用のネットワークエミュレータ（テスト用CLIツール）。意図的なパケットロス、遅延、入力偽装などを自動生成します。

### Headless Mode (ヘッドレスモード)
CUI画面やインタラクティブな入力を一切持たず、起動引数 `--host` や `--hash [CCTR_HASH]` に従って自動的につながり、対戦終了後に自動でプロセス終了する完全自動運転のCLI動作モード。主として自動インテグレーションテストで使用されます。
