Fixed an issue where the game would end unexpectedly after a series of draws.
Made numerous minor adjustments to HUD elements across various screens.
Fixed an issue related to save-state handling in Training Mode.
Fixed an issue where the BGM playback position would reset when resetting Training Mode.
Resolved synchronization issues occurring during the loading or intro screens at the start of matches in Spectator Mode.
Added a field to the GUI for entering player names.
Fixed an issue where launching the game via CLI (command line) caused the user to be recognized as "Player 2"; while controller settings could be configured, they were not reflected in the game.
Fixed an issue preventing IPv6 connections (Note: IPv6 hole punching is currently only possible via IPv4).
Added an automatic replay saving feature.
Added a replay playback feature.
Implemented various other minor fixes.
