# 実コントローラ手操作確認（2026-09-11）

ユーザーの手操作と、ローカル接続した実ゲーム2プロセスで確認。接続デバイスはXbox One For Windows。スクリプト入力は無効。確認範囲はキャラクター選択画面とF4設定画面。

## 確認結果

- 接続検出とPLAYER 1への割り当てができた。
- 通常の方向・決定ボタンを入力時計が読み、公開バッファへ保存した。画面上でキャラクター移動とアルクェイドのスタイル選択への進行を確認した。
- 双方で比較可能な適用入力27233フレーム、うち非ニュートラル627フレーム。入力列の不一致は0件。
- F4設定中の非ゼロ生入力変化134件に対し、非ゼロ公開値は0件。設定中のゲーム適用入力への漏れも0件。
- 設定を閉じた後の通常入力復帰を確認。セッションのFAILED記録なし。
- 押しっぱなしでF4を閉じるケースは未確認。実測された閉じる瞬間はすべて生入力がニュートラルだったため、押しっぱなし保護を実機確認済みとはしない。
- 今回は戦闘中の手操作・実回線越しの遅延試験は行っていない。前回のスクリプト入力による巻き戻し疎通とは分けて扱う。

## 設定と後片付け

初期テスト設定はキーボード。今回コントローラをP1へ割り当てた。設定中のボタン操作は割り当て登録も開始するため、途中に複数の項目が上方向へ変更された保存内容を検出した。終了前に最新ファイルを再確認すると上下左右と各ボタンは個別の内容へ変更されており、その最新設定を保持した。上書きによる初期化は行っていない。

今回起動したゲームとランチャーのみ終了。ログ・開始前後の設定・終了時のコントローラ設定を `I:\work_space\CCCaster_v10\build_logs\manual_controller_20260911_003326` に保存した。

## 観測用の変更

`CCCASTER_INPUT_TRACE=1`のときだけ、生入力・公開値・設定画面の開閉状態を変化時に記録し、ゲーム適用入力を記録する。自動操作を有効にせず物理入力を観測できるようにした。既定では無効。通信版7、入力処理は変更していない。観測追加後のビルド成功、入力時計の単体確認成功。
