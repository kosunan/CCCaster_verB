# キャラクター未選択時のキャンセル禁止

2026-09-11。通信版8。

キャラクター選択画面で、各プレイヤーの選択段階がキャラクター選択（0）のときだけBとCANCELを除去する。ムーン・カラー選択中のキャンセル、方向・決定、対戦中のBは変更しない。

旧実装 OLD/targets/DllNetplayManager.cpp の終了防止条件を確認。現行では RealGameMemory::WriteInput で、入力を実際にゲームへ渡すフレームのP1/P2の選択段階をそれぞれ読む。通信スレッドから選択状態を読み取らず、先読み入力とゲーム進行の時間差を避ける。中央バッファには送受信した入力を保持する。

Releaseビルド成功。今回の変更後の実コントローラ操作は未確認。
双方でEXE/DLLを更新する。既存のコントローラ設定はそのまま使用する。
